[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "EyeSimulator"
description = "Easy-to-use interface for optical simulations of the eye."
authors = [
    { name = "Corné Haasjes", email = "c.haasjes@lumc.nl" },
    { name = "Jan-Willem Beenakker", email = "j.w.m.beenakker@lumc.nl" },
]
version = "0.0.1"
dependencies = [
    "zospy"
]

[tool.hatch.envs.default]
python = "3.11"
skip-install = true

[tool.black]
line-length = 120

[tool.isort]
profile = "black"